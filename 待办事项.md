# 待办事项

以 `Skill/character-storyboard-generator` 的规格（V1.0）与当前 `Sample/` 内容比对后，整理出以下尚未完成的工作。

## 1. 正式项目结构尚未建立

- [ ] 尚未依 `references/naming-and-layout.md` 建立正式的 `<project>/` 目录（`source/`、`identity/`、`manifests/`、`成果/`、`_rejected/`、`project.json`），目前只有 `Sample/` 这个教学范例。
- [ ] `assets/project-template.json` 里的 `character_id` 仍是占位符 `"replace-me"`，尚未针对实际角色（例如 `xiaomai`）建立一份正式的 `project.json`。
- [ ] 尚未制作正式的「身份定妆图」（`identity/approved-reference.png`），只有 `Sample/原图.png`（＝ `happy.png`）这张原始输入图。

## 2. 动作 × 表情覆盖率很低

- [ ] `references/poses.json` 共 30 个动作、`references/expressions.json` 共 40 个表情，但 `Sample/成果/` 目前只有 **21 张**范例图，远未覆盖完整组合（即使只算 priority 1 的动作与表情，也还有大量组合未生成）。
- [ ] 尚未依「批次策略」（calibration → core → extended → repair）跑完整批次；目前的 15 张图偏向精选展示，不是规范定义的 calibration 校准集（`P01/P06/P16/P17/P20/P22` × `E01/E02/E03/E05/E22/E21`）。
- [ ] `E20-charming-sexy`（成人限定表情）尚未处理：需要先确认角色 `adult_status` 是否为 `adult`，若非成人或年龄不明，则维持停用状态，改用 `E19-charming` 等替代。

## 3. 脚本尚未实际跑过

- [ ] `scripts/build_manifest.py` 尚未针对正式项目执行过，缺少 `manifests/tasks.jsonl`、`manifests/tasks.csv`。
- [ ] `scripts/audit_outputs.py` 尚未执行过，缺少 `manifests/audit.json` 审核报告（缺档、误命名、重复、损毁、尺寸错误、非透明背景等检查）。

## 4. 素材整理

- [ ] `Sample/_chroma/` 内有 9 张中间抠图过程文件（chroma-key 工作文件），目前仍留在仓库中，需要确认是否要保留在公开仓库、或搬到 `.gitignore`／独立说明其用途。
- [ ] 根目录 `happy.png` 与 `Sample/原图.png` 是完全相同的文件（重复），可考虑只保留一份并在文件说明中标注引用关系。

## 5. 文档与发布

- [ ] 尚未加入 LICENSE（开源授权条款），公开仓库建议补上。
- [ ] 尚未提供多角色范例，目前只有 `xiaomai` 一个角色的示范成果。
- [ ] 尚未建立自动化测试或 CI，验证 `build_manifest.py` / `audit_outputs.py` 在不同 `project.json` 输入下的正确性。
